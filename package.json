{
  "private": true,
  "name": "react-asyncmodule-packages",
  "version": "0.1.0",
  "devDependencies": {
    "coveralls": "^3.0.2",
    "lerna": "^2.11.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "selfpulbish": "lerna publish —dist-tag beta --skip-git"
  },
  "dependencies": {}
}
