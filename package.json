{
  "private": true,
  "name": "react-asyncmodule-packages",
  "version": "0.1.0",
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build"
  }
}